---
description: Serve the site locally for preview.
---

1. Start a static web server in the project root.

// turbo
```bash
python3 -m http.server 8000
```

2. Tell the user to open http://localhost:8000 in their browser, and to press Ctrl+C in the terminal to stop the server when finished.
