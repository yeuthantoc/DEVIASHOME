# n8n webhook that receives lead-form submissions (server-only, never exposed).
# Set the real value in .env.local (which is gitignored).
N8N_WEBHOOK_URL=https://automation.jobbim.vn/webhook/your-webhook-id
